[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["cd astro-solar && npm ci"]

[phases.build]
cmds = ["cd astro-solar && npm run build"]

[variables]
HOST = "0.0.0.0"
PORT = "3000"

[start]
cmd = "cd astro-solar && node ./dist/server/entry.mjs"
